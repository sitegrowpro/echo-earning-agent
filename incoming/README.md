# Drop your packs here, then tell me.

- `incoming/audio/` — WAVs (Tier 1)
- `incoming/textures/` — PBR sets, one folder per texture (Tier 2)
- `incoming/models/` — GLB/GLTF + sidecar textures (Tier 3)

Keep original filenames. The agent wires everything into the game from here.
